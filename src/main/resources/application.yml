spring:
  application:
    name: ebting-server

  datasource:
    url: jdbc:mysql://localhost:3306/ebting?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: sql@1q2w3e
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: your-secret-key-minimum-256-bits-long-for-hs256-algorithm-security-purpose
  access-token-validity: 86400000  # 24시간 (밀리초)

fastapi:
  url: http://localhost:8000

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    tags-sorter: alpha
    operations-sorter: alpha
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  show-actuator: false